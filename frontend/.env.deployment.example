# Copy to .env.production.local for a build outside Docker.
# Empty uses the same origin with nginx proxying /api and /uploads.
# For a separately hosted API set https://api.your-domain.com (without /api).
REACT_APP_API_BASE_URL=
GENERATE_SOURCEMAP=false
